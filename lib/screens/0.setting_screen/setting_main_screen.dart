import 'package:flutter_common_app/utilities/index.dart';

class SettingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    logger.i('SettingScreen');
    return Center(
      child: Container(
        child: Text('SettingScreen'),
      ),
    );
  }
}